# name: Deploy Backend to EC2

# on:
#   push:
#     paths:
#       - 'backend/**'
#       - '.github/workflows/deploy-backend.yml'
#     branches:
#       - main
#   workflow_dispatch:

# jobs:
#   deploy:
#     name: Deploy Backend
#     runs-on: ubuntu-latest
    
#     steps:
#       - name: Checkout code
#         uses: actions/checkout@v4

#       - name: Configure AWS Credentials
#         uses: aws-actions/configure-aws-credentials@v4
#         with:
#           aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
#           aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
#           aws-region: us-east-1

#       - name: Setup Node.js
#         uses: actions/setup-node@v4
#         with:
#           node-version: '18'

#       - name: Install dependencies
#         working-directory: backend
#         run: npm ci

#       - name: Create deployment package
#         working-directory: backend
#         run: |
#           zip -r ../backend-deployment.zip . -x "*.git*" "node_modules/*" "tests/*"

#       - name: Upload to S3
#         run: |
#           aws s3 cp backend-deployment.zip s3://${{ secrets.DEPLOYMENT_BUCKET }}/backend/backend-${GITHUB_SHA}.zip

#       - name: Deploy to EC2 via SSM
#         run: |
#           aws ssm send-command \
#             --document-name "AWS-RunShellScript" \
#             --targets "Key=tag:Application,Values=sincerra-backend" \
#             --parameters 'commands=[
#               "cd /opt/sincerra-backend",
#               "aws s3 cp s3://${{ secrets.DEPLOYMENT_BUCKET }}/backend/backend-${GITHUB_SHA}.zip /tmp/backend.zip",
#               "unzip -o /tmp/backend.zip -d /opt/sincerra-backend",
#               "npm install --production",
#               "pm2 restart sincerra-backend || pm2 start src/server.js --name sincerra-backend",
#               "rm /tmp/backend.zip"
#             ]' \
#             --comment "Deploy backend version ${GITHUB_SHA}"
